# Calibration
This is used to calibrate any camera with the required info.

Can be used with any camera, put the images of the calibration in the folder ./images

Then run, calib.py

Use the output from this to update: ../config.json
<br> (ie. the base directory/config.json)